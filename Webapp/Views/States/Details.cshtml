@model Entities.State

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>State</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            Bandeira
        </dt>
        <dd class = "col-sm-10">
            <img style="width: auto; height: 100px;" src="@Model.FlagUrl" title="Bandeira do @Model.Name" alt="Bandeira do @Model.Name" />
        </dd>
        <dt class="col-sm-2">
            Pais
        </dt>
        <dd class="col-sm-2">
            @Model.GetCountryName()
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Back to List</a>
</div>
